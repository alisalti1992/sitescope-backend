{
  "name": "sitescope-backend",
  "version": "0.0.1",
  "description": "This is an example of a Crawlee project.",
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "crawlee": "^3.0.0",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "puppeteer": "*",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "concurrently \"npm run prisma:dev\" \"npm run dev:nodemon\" ",
    "dev:nodemon": "nodemon src/server.js",
    "prisma:dev": "npx prisma dev",
    "prisma:migrate": "npx prisma migrate dev reset",
    "test": "echo \"Error: oops, the actor has no tests yet, sad!\" && exit 1"
  },
  "author": "It's not you it's me",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10",
    "prisma": "^6.14.0"
  }
}
